Decompose this case using the fixed driver catalog.

Required tasks:
1. Return an assessment for every catalog driver.
2. For relevant drivers, return a normalized distribution over every category,
   evidence, assumptions, evidence sufficiency, and up to three questions that
   would most reduce uncertainty.
3. Check whether the basis covers all material technical and organizational
   implementation work.
4. Create requests_to_add for uncovered work without modifying the catalog.

CATALOG ID: {catalog_id}
CATALOG VERSION: {catalog_version}
CATALOG SHA-256: {catalog_sha256}

CATALOG
{catalog}

CASE ID: {case_id}
CASE PATH: {case_path}

CASE TEXT
{case_text}
